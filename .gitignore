# hist_cache.json.gz is intentionally committed — it contains compiled historical
# data that GitHub Actions needs.  Do NOT add it here.
*.xlsb
*.xlsx
*.pptx
__pycache__/
*.pyc

# Local pipeline artefacts — never commit
TVS/tvs_last_payload.json
tvs_last_payload.json
TVS/push_tvs_data.log
push_tvs_data.log

# Claude Code local-machine config
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Other-project files co-located in this working directory — keep out of TVS repo
HMSI*.html
HMSI*.md
Jawa_Yezdi*.html
Model_Retail_Engine*.html
.claude/

# Scratch files
extract_head.txt
extract_utils.txt
